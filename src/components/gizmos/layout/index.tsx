import "../gizmos.css";
import "./layout.css";

export const ContainerGizmo = (props: any) => {
  return (
    <div className="gizmo container" {...props}>
      <div />
    </div>
  );
};

export const SectionGizmo = (props: any) => {
  return (
    <div className="gizmo section" {...props}>
      <div />
    </div>
  );
};
